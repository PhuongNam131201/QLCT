<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/blue" /> <!-- Màu nền -->
    <corners android:radius="16dp" /> <!-- Bán kính góc bo -->
    <stroke android:width="2dp" android:color="@color/blue" /> <!-- Viền (tuỳ chọn) -->
</shape>
